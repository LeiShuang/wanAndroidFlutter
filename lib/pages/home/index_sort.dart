import 'package:flutter/material.dart';

class KnowledgeSortPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('体系'),
      ),
    );
  }

}